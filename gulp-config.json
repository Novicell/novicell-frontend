{
    "tasksToRun": ["scripts"],

    "styles": {
        "baseDir": "less",
        "dist": "css/compiled"
    },

    "scripts": {
        "baseDir": "scripts",
        "dist": "scripts/compiled"
    }
}