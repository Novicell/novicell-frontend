<input
    class="input {{ modifier }} {{ class }}"
    type="{{ type }}"
    name="{{ input.name }}"
    id="{{ input.id }}"
    placeholder="{{ input.placeholder }}" {{#if required }} required="{{ required }}" {{/if}} />