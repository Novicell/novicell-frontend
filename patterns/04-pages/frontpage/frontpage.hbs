{{ render '@header' }}

{{#extend '@main' }}
  {{#content 'main-content' }}
    {{#extend '@grid-container--site-width'}}
      {{#content 'grid-container-content'}}
        {{ render '@about-boxes' }}
        {{ render '@primary-boxes' }}
        {{ render '@feedback-box' }}
        {{ render '@secondary-boxes' }}
      {{/content}}
    {{/extend}}
  {{/content}} 
{{/extend}}

{{ render '@footer' }}